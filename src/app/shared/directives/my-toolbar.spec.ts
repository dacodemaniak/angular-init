import { MyToolbar } from './my-toolbar';

describe('MyToolbar', () => {
  it('should create an instance', () => {
    const directive = new MyToolbar();
    expect(directive).toBeTruthy();
  });
});
